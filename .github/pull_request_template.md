## O que este PR muda

<!-- Resumo curto. -->

## Checklist

- [ ] `./gradlew check` verde e `git diff --check` limpo
- [ ] Não enfraquece a regra external-only em nenhuma das três camadas
      (docs, UI/CLI, núcleo) — ver CONTRIBUTING.md
- [ ] Regras novas de segurança/produto têm teste unitário
- [ ] Mudança em JNA/comportamento nativo: descrevi o teste em máquina real
      (chip, macOS, monitor, conexão) ou marquei que não se aplica

## Teste em máquina real (se aplicável)

<!-- O que foi executado e o resultado observado. -->
